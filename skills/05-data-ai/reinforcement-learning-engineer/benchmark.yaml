---
name: reinforcement-learning-engineer
version: 1

tests:
  - id: default
    prompt: "Perform your role as reinforcement-learning-engineer. Context: Use when a task needs RL environment design, policy training, reward engineering, or deployment of decision-making agents."
    expected:
      return_contract: true
      checks:
        - "returns structured output matching SKILL.md Return section"
        - "validates at least one success path"
        - "validates at least one failure path"
        - "does not exceed scope boundaries"
    eval:
      method: llm-as-judge
      pass_threshold: 0.8
---
