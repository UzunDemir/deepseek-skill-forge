---
name: embedded-systems
version: 1

tests:
  - id: default
    prompt: "Perform your role as embedded-systems. Context: Use when a task needs embedded or hardware-adjacent work involving device constraints, firmware boundaries, timing, or low-level integration."
    expected:
      return_contract: true
      checks:
        - "returns structured output matching SKILL.md Return section"
        - "validates at least one success path"
        - "validates at least one failure path"
        - "does not exceed scope boundaries"
    eval:
      method: llm-as-judge
      pass_threshold: 0.8
---
