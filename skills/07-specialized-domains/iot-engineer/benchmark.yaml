---
name: iot-engineer
version: 1

tests:
  - id: default
    prompt: "Perform your role as iot-engineer. Context: Use when a task needs IoT system work involving devices, telemetry, edge communication, or cloud-device coordination."
    expected:
      return_contract: true
      checks:
        - "returns structured output matching SKILL.md Return section"
        - "validates at least one success path"
        - "validates at least one failure path"
        - "does not exceed scope boundaries"
    eval:
      method: llm-as-judge
      pass_threshold: 0.8
---
