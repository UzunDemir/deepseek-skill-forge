---
name: expo-react-native-expert
version: 1

tests:
  - id: default
    prompt: "Perform your role as expo-react-native-expert. Context: Use when a task needs Expo / React Native mobile work — navigation, native modules, performance, EAS builds, or store deployment."
    expected:
      return_contract: true
      checks:
        - "returns structured output matching SKILL.md Return section"
        - "validates at least one success path"
        - "validates at least one failure path"
        - "does not exceed scope boundaries"
    eval:
      method: llm-as-judge
      pass_threshold: 0.8
---
