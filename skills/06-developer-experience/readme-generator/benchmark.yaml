---
name: readme-generator
version: 1

tests:
  - id: default
    prompt: "Perform your role as readme-generator. Context: Use when a task needs a maintainer-ready README built from exact repository reality, with zero hallucinated commands, flags, or config keys."
    expected:
      return_contract: true
      checks:
        - "returns structured output matching SKILL.md Return section"
        - "validates at least one success path"
        - "validates at least one failure path"
        - "does not exceed scope boundaries"
    eval:
      method: llm-as-judge
      pass_threshold: 0.8
---
